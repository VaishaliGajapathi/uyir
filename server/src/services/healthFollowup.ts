export async function scheduleHealthReminders(donorId: string, donationDate: Date) {
  return { ok: true };
}
export async function generateHealthReminders(donorId: string, donationDate: Date) {
  return [];
}
